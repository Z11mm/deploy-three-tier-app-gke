pipeline {
  agent any
  tools {nodejs "Node"}

  stages {

    stage('Build') {
      steps {
        echo 'Building application'
        // sh '''
        // #!/bin/bash
        // rm -rf *.tar.gz
        // npm install
        // npm run build
        // tar czf react-build.tar.gz /build
        // '''
        echo 'complete'
      }
    }
  }
}